<div class="info-window">
  <strong><%= link_to flat.title, flat_path(flat.id), class: "marker-link" %></strong><br>
  <%= flat.location %><br>
  <strong><%= flat.price_per_night %></strong>€ per night
</div>
